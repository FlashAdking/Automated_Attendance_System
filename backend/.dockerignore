# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments — never copy into the image
attend_snap/
venv/
env/
.venv/

# Tests & CI
tests/
pytest.ini
.pytest_cache/
.coverage
htmlcov/

# Dev / editor
.env
.env.*
*.DS_Store
.idea/
.vscode/

# Assets / temp uploads (large files not needed in image)
assets/

# .h5 model — production uses .onnx via onnxruntime (no tensorflow in image)
app/face_recognition/ML_models/*.h5

# One-time conversion scripts — not needed at runtime
scripts/
